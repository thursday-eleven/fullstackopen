```mermaid
sequenceDiagram
participant browser
participant server

Note right of browser: User enters
browser ->> server: GET https://studies.cs.helsinki.fi/exampleapp/spa
server ->> browser: html site, main.css, spa.js, data.json
Note right of browser: notes rendered via spa.js



```